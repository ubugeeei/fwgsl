{
  "name": "shadml",
  "displayName": "shadml — Functional WGSL",
  "description": "Language support for shadml, a pure functional language for WebGPU that compiles to WGSL.",
  "version": "0.1.0",
  "publisher": "shadml",
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:shadml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "shadml",
        "aliases": [
          "shadml",
          "Functional WGSL"
        ],
        "extensions": [
          ".shadml"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "shadml",
        "scopeName": "source.shadml",
        "path": "./syntaxes/shadml.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "shadml",
      "properties": {
        "shadml.server.path": {
          "type": "string",
          "default": "shadml-lsp",
          "description": "Path to the shadml language server executable."
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  }
}
