'use strict';

// Generated from examples/shadorial/*.fwgsl
window.FWGSL_EMBEDDED_EXAMPLES = {
    "shadorial-01": "-- Shadorial Chapter 01: Hello Shader\n-- UV coordinates mapped to RGB color\n\nshade : Vec 2 F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord resolution = vec4 uv.x uv.y 0.5 1.0\n  where\n    uv = fragCoord / resolution\n",
    "shadorial-02": "-- Shadorial Chapter 02: Uniforms\n-- Time animation, resolution, and mouse interaction\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution mouse = vec4 color.x color.y color.z 1.0\n  where\n    uv     = fragCoord / resolution\n    mouseN = mouse / resolution\n    r      = 0.5 + 0.5 * sin time\n    g      = 0.5 + 0.5 * sin (time + 2.094)\n    b      = 0.5 + 0.5 * sin (time + 4.189)\n    dist   = length (uv - mouseN)\n    circle = smoothstep 0.15 0.14 dist\n    base   = vec3 r g b\n    intensity = 0.3 + circle * 0.7\n    color  = base * splat3 intensity\n",
    "shadorial-03": "-- Shadorial Chapter 03: Colors & Gradients\n-- Animated 3-color gradient with sine wave distortion\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 color'.x color'.y color'.z 1.0\n  where\n    uv     = fragCoord / resolution\n    colorA = vec3 0.2 0.0 0.5\n    colorB = vec3 1.0 0.4 0.0\n    colorC = vec3 0.0 0.8 0.6\n    t      = uv.x + sin (uv.y * 3.0 + time) * 0.2\n    color  = mix colorA colorB (smoothstep 0.0 0.5 t)\n    color' = mix color  colorC (smoothstep 0.5 1.0 t)\n",
    "shadorial-04": "-- Shadorial Chapter 04: Sin Wave\n-- Sine wave rendering with amplitude, frequency, phase\n\ndrawWave : F32 -> F32 -> F32 -> F32 -> F32\ndrawWave y wave lineWidth opacity = line * opacity\n  where\n    dist = abs (y - wave)\n    line = 1.0 - smoothstep 0.0 lineWidth dist\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 color''.x color''.y color''.z 1.0\n  where\n    uv        = fragCoord / resolution\n    x         = uv.x * 10.0\n    y         = uv.y * 2.0 - 1.0\n    amplitude = 0.5\n    frequency = 1.0\n    phase     = time * 2.0\n    wave1     = amplitude * sin (frequency * x + phase)\n    wave2     = 0.3 * sin (2.0 * x + phase * 0.7)\n    lineWidth = 0.03\n    line1     = drawWave y wave1 lineWidth 1.0\n    line2     = drawWave y wave2 lineWidth 0.8\n    axis      = 1.0 - smoothstep 0.0 0.005 (abs y)\n    bgColor   = vec3 0.05 0.05 0.05\n    color     = mix bgColor (vec3 0.3 0.3 0.3) (axis * 0.5)\n    color'    = mix color (vec3 1.0 0.4 0.3) line2\n    color''   = mix color' (vec3 0.2 0.6 1.0) line1\n",
    "shadorial-05": "-- Shadorial Chapter 05: Saw & Triangle Wave\n-- Sawtooth and triangle waves using fract and abs\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 color''.x color''.y color''.z 1.0\n  where\n    uv        = fragCoord / resolution\n    x         = uv.x * 6.0 + time\n    y         = uv.y * 2.0 - 1.0\n    saw       = fract x * 2.0 - 1.0\n    tri       = abs (fract x * 2.0 - 1.0) * 2.0 - 1.0\n    lineWidth = 0.03\n    distSaw   = abs (y - saw * 0.5)\n    distTri   = abs (y - tri * 0.5)\n    lineSaw   = 1.0 - smoothstep 0.0 lineWidth distSaw\n    lineTri   = 1.0 - smoothstep 0.0 lineWidth distTri\n    axis      = 1.0 - smoothstep 0.0 0.005 (abs y)\n    bgColor   = vec3 0.05 0.05 0.05\n    color     = mix bgColor (vec3 0.3 0.3 0.3) (axis * 0.5)\n    color'    = mix color (vec3 1.0 0.5 0.1) lineSaw\n    color''   = mix color' (vec3 0.3 0.9 0.5) lineTri\n",
    "shadorial-06": "-- Shadorial Chapter 06: Pulse Wave\n-- Square/pulse wave with animated duty cycle\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 final.x final.y final.z 1.0\n  where\n    uv        = fragCoord / resolution\n    x         = uv.x * 6.0 + time\n    y         = uv.y * 2.0 - 1.0\n    duty      = 0.5 + 0.3 * sin (time * 0.8)\n    pulse     = step (fract x) duty * 2.0 - 1.0\n    lineWidth = 0.025\n    distH     = abs (y - pulse * 0.4)\n    lineH     = 1.0 - smoothstep 0.0 lineWidth distH\n    fractX    = fract x\n    edgeRise  = if abs fractX < 0.02 then 1.0 else 0.0\n    edgeFall  = if abs (fractX - duty) < 0.02 then 1.0 else 0.0\n    vertLine  = (edgeRise + edgeFall) * step (abs y) 0.4\n    axis      = 1.0 - smoothstep 0.0 0.005 (abs y)\n    gridX     = 1.0 - smoothstep 0.0 0.008 (abs (fract x - 0.5))\n    gridY     = 1.0 - smoothstep 0.0 0.008 (abs (fract (uv.y * 4.0) - 0.5))\n    bgColor   = vec3 0.05 0.05 0.05\n    color     = mix bgColor (vec3 0.1 0.1 0.1) ((gridX + gridY) * 0.3)\n    color'    = mix color (vec3 0.3 0.3 0.3) (axis * 0.5)\n    color''   = mix color' (vec3 0.9 0.3 0.6) lineH\n    color'''  = mix color'' (vec3 0.9 0.3 0.6) (vertLine * 0.9)\n    dutyBar   = step uv.y 0.05 * step uv.x duty\n    final     = mix color''' (vec3 0.45 0.15 0.3) dutyBar\n",
    "shadorial-07": "-- Shadorial Chapter 07: Noise Wave\n-- 1D value noise with cubic Hermite interpolation\n\nhash : F32 -> F32\nhash n = fract (sin n * 43758.5453123)\n\nnoise1d : F32 -> F32\nnoise1d x = mix (hash i) (hash (i + 1.0)) u\n  where\n    i = floor x\n    f = fract x\n    u = f * f * (3.0 - 2.0 * f)\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 color''.x color''.y color''.z 1.0\n  where\n    uv        = fragCoord / resolution\n    x         = uv.x * 8.0\n    y         = uv.y * 2.0 - 1.0\n    noiseWave = noise1d (x * 2.0 + time) * 0.6 - 0.3\n              + noise1d (x * 5.0 - time * 0.7) * 0.2\n    lineWidth = 0.03\n    dist      = abs (y - noiseWave)\n    line      = 1.0 - smoothstep 0.0 lineWidth dist\n    sineWave  = 0.3 * sin (x * 2.0 + time)\n    distSine  = abs (y - sineWave)\n    lineSine  = 1.0 - smoothstep 0.0 (lineWidth * 0.7) distSine\n    axis      = 1.0 - smoothstep 0.0 0.005 (abs y)\n    bgNoise   = noise1d (uv.x * 10.0 + time * 0.3) * 0.1\n    bgColor   = vec3 (0.05 + bgNoise) (0.05 + bgNoise) (0.05 + bgNoise)\n    color     = mix bgColor (vec3 0.3 0.3 0.3) (axis * 0.4)\n    color'    = mix color (vec3 0.3 0.3 0.5) (lineSine * 0.4)\n    color''   = mix color' (vec3 0.4 0.9 0.8) line\n",
    "shadorial-08": "-- Shadorial Chapter 08: Wave Composition\n-- Additive synthesis with harmonic series (square wave approximation)\n\ndrawLine : F32 -> F32 -> F32 -> F32\ndrawLine y wave width =\n  1.0 - smoothstep 0.0 width (abs (y - wave))\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 final.x final.y final.z 1.0\n  where\n    uv        = fragCoord / resolution\n    x         = uv.x * 8.0\n    y         = uv.y * 2.0 - 1.0\n    lineWidth = 0.025\n    h1        = sin (x + time)\n    h3        = sin (3.0 * x + time * 1.5) / 3.0\n    h5        = sin (5.0 * x + time * 2.0) / 5.0\n    h7        = sin (7.0 * x + time * 2.5) / 7.0\n    composite = (h1 + h3 + h5 + h7) * 0.5\n    lineH1    = drawLine y (h1 * 0.4) lineWidth\n    lineH3    = drawLine y (h3 * 0.4) (lineWidth * 0.7)\n    lineH5    = drawLine y (h5 * 0.4) (lineWidth * 0.7)\n    lineH7    = drawLine y (h7 * 0.4) (lineWidth * 0.7)\n    lineComp  = drawLine y composite lineWidth\n    axis      = 1.0 - smoothstep 0.0 0.005 (abs y)\n    bgColor   = vec3 0.05 0.05 0.05\n    color     = mix bgColor (vec3 0.25 0.25 0.25) (axis * 0.5)\n    color'    = mix color (vec3 0.3 0.3 0.6) (lineH1 * 0.5)\n    color''   = mix color' (vec3 0.3 0.5 0.3) (lineH3 * 0.5)\n    color'''  = mix color'' (vec3 0.5 0.3 0.3) (lineH5 * 0.5)\n    color'''' = mix color''' (vec3 0.4 0.4 0.2) (lineH7 * 0.5)\n    final     = mix color'''' (vec3 1.0 0.8 0.3) lineComp\n",
    "shadorial-09": "-- Shadorial Chapter 09: Shapes & SDF\n-- Signed Distance Functions for circle, rounded rect, and triangle\n\nsdCircle : Vec 2 F32 -> F32 -> F32\nsdCircle p r = length p - r\n\nsdRoundedRect : Vec 2 F32 -> Vec 2 F32 -> F32 -> F32\nsdRoundedRect p b r = length (max q (vec2 0.0 0.0)) + min (max q.x q.y) 0.0 - r\n  where\n    q = abs p - b + vec2 r r\n\nsdTriangle : Vec 2 F32 -> F32 -> F32\nsdTriangle p r = -(length p'') * sign p''.y\n  where\n    k   = sqrt 3.0\n    px  = abs p.x - r\n    py  = p.y + r / k\n    p'  = if px + k * py > 0.0\n            then vec2 ((px - k * py) / 2.0) (((-k) * px - py) / 2.0)\n            else vec2 px py\n    p'' = vec2 (p'.x - clamp p'.x (-2.0 * r) 0.0) p'.y\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 final.x final.y final.z 1.0\n  where\n    halfRes   = resolution / splat2 2.0\n    uv        = (fragCoord - halfRes) / splat2 resolution.y\n    t         = time * 0.8\n    circlePos = vec2 (-0.35 + 0.03 * sin t) (0.03 * cos (t * 1.3))\n    rectPos   = vec2 (0.03 * cos (t * 0.9)) (0.03 * sin (t * 1.1))\n    triPos    = vec2 (0.35 + 0.03 * sin (t * 1.2)) (0.03 * cos (t * 0.7))\n    dCircle   = sdCircle (uv - circlePos) 0.15\n    dRect     = sdRoundedRect (uv - rectPos) (vec2 0.12 0.10) 0.03\n    dTri      = sdTriangle (uv - triPos) 0.15\n    dUnion    = min dCircle (min dRect dTri)\n    bands     = 0.5 + 0.5 * cos (dUnion * 80.0)\n    edgeTint  = vec3 0.1 0.1 0.1 * splat3 (sign dUnion)\n    bgBase    = vec3 0.95 0.95 0.95 - edgeTint\n    bgColor   = bgBase * splat3 (0.8 + 0.2 * bands)\n    pixelSize = 1.5 / resolution.y\n    fillC     = 1.0 - smoothstep 0.0 pixelSize dCircle\n    fillR     = 1.0 - smoothstep 0.0 pixelSize dRect\n    fillT     = 1.0 - smoothstep 0.0 pixelSize dTri\n    color     = mix bgColor (vec3 0.2 0.6 0.9) fillC\n    color'    = mix color (vec3 0.9 0.4 0.3) fillR\n    color''   = mix color' (vec3 0.3 0.8 0.4) fillT\n    outlineC  = smoothstep pixelSize (pixelSize * 2.0) (abs dCircle - 0.005)\n    outlineR  = smoothstep pixelSize (pixelSize * 2.0) (abs dRect - 0.005)\n    outlineT  = smoothstep pixelSize (pixelSize * 2.0) (abs dTri - 0.005)\n    outline   = min outlineC (min outlineR outlineT)\n    final     = color'' * splat3 (0.3 + 0.7 * outline)\n",
    "shadorial-10": "-- Shadorial Chapter 10: Light & Reflection\n-- Phong lighting model with Fresnel rim on a ray-traced sphere\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let halfRes      = resolution / splat2 2.0\n      uv           = (fragCoord - halfRes) / splat2 resolution.y\n      sphereCenter = vec3 0.0 0.0 0.0\n      sphereRadius = 0.35\n      lt           = time * 0.7\n      lightPos     = vec3 (0.8 * cos lt) (0.5 * sin (lt * 0.8)) 1.0\n      rayOrigin    = vec3 uv.x uv.y 2.0\n      rayDir       = vec3 0.0 0.0 -1.0\n      oc           = rayOrigin - sphereCenter\n      b            = dot oc rayDir\n      c            = dot oc oc - sphereRadius * sphereRadius\n      discriminant = b * b - c\n      bgColor      = mix (vec3 0.08 0.08 0.15) (vec3 0.15 0.1 0.2) (uv.y + 0.5)\n  in if discriminant < 0.0\n       then vec4 bgColor.x bgColor.y bgColor.z 1.0\n       else\n         let t         = (-b) - sqrt discriminant\n             hitPoint  = rayOrigin + rayDir * splat3 t\n             normal    = normalize (hitPoint - sphereCenter)\n             lightDir  = normalize (lightPos - hitPoint)\n             viewDir   = normalize (rayOrigin - hitPoint)\n             reflDir   = reflect (negate lightDir) normal\n             matColor  = vec3 0.3 0.5 0.8\n             lightCol  = vec3 1.0 0.95 0.85\n             ambient   = lightCol * matColor * splat3 0.1\n             diff      = max (dot normal lightDir) 0.0\n             diffuse   = lightCol * matColor * splat3 diff\n             spec      = pow (max (dot viewDir reflDir) 0.0) 64.0\n             specular  = lightCol * splat3 (0.8 * spec)\n             fresnel   = pow (1.0 - max (dot normal viewDir) 0.0) 3.0\n             fresnelC  = vec3 0.4 0.5 0.7 * splat3 (fresnel * 0.5)\n             color     = ambient + diffuse + specular + fresnelC\n         in vec4 color.x color.y color.z 1.0\n",
    "shadorial-11": "-- Shadorial Chapter 11: Animation\n-- 6 easing functions visualized as animated balls on tracks\n\neaseInQuad : F32 -> F32\neaseInQuad t = t * t\n\neaseOutQuad : F32 -> F32\neaseOutQuad t = t * (2.0 - t)\n\neaseInOutCubic : F32 -> F32\neaseInOutCubic t =\n  if t < 0.5\n    then 4.0 * t * t * t\n    else 1.0 - pow (-2.0 * t + 2.0) 3.0 / 2.0\n\neaseOutElastic : F32 -> F32\neaseOutElastic t =\n  if t == 0.0 then 0.0\n  else if t == 1.0 then 1.0\n  else pow 2.0 (-10.0 * t) * sin ((t * 10.0 - 0.75) * (2.0 * 3.14159 / 3.0)) + 1.0\n\neaseOutBounce : F32 -> F32\neaseOutBounce t =\n  if t < 1.0 / 2.75\n    then 7.5625 * t * t\n    else if t < 2.0 / 2.75\n      then let t' = t - 1.5 / 2.75\n           in 7.5625 * t' * t' + 0.75\n      else if t < 2.5 / 2.75\n        then let t' = t - 2.25 / 2.75\n             in 7.5625 * t' * t' + 0.9375\n        else let t' = t - 2.625 / 2.75\n             in 7.5625 * t' * t' + 0.984375\n\ncircle : Vec 2 F32 -> Vec 2 F32 -> F32 -> F32 -> F32\ncircle uv center radius pixelSize =\n  1.0 - smoothstep (radius - pixelSize) (radius + pixelSize) (length (uv - center))\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution = vec4 final''.x final''.y final''.z 1.0\n  where\n    halfRes = resolution / splat2 2.0\n    uv     = (fragCoord - halfRes) / splat2 resolution.y\n    period = 3.0\n    t      = mod time period / period\n    r      = 0.03\n    startX = -0.4\n    endX   = 0.4\n    px     = 1.5 / resolution.y\n    x1     = mix startX endX t\n    x2     = mix startX endX (easeInQuad t)\n    x3     = mix startX endX (easeOutQuad t)\n    x4     = mix startX endX (easeInOutCubic t)\n    x5     = mix startX endX (easeOutElastic t)\n    x6     = mix startX endX (easeOutBounce t)\n    c1     = circle uv (vec2 x1 0.30) r px\n    c2     = circle uv (vec2 x2 0.18) r px\n    c3     = circle uv (vec2 x3 0.06) r px\n    c4     = circle uv (vec2 x4 -0.06) r px\n    c5     = circle uv (vec2 x5 -0.18) r px\n    c6     = circle uv (vec2 x6 -0.30) r px\n    bgColor = vec3 0.05 0.05 0.08\n    color   = mix bgColor (vec3 0.9 0.3 0.3) c1\n    color'  = mix color (vec3 0.9 0.6 0.2) c2\n    color'' = mix color' (vec3 0.3 0.9 0.3) c3\n    final   = mix color'' (vec3 0.3 0.6 0.9) c4\n    final'  = mix final (vec3 0.8 0.3 0.8) c5\n    final'' = mix final' (vec3 0.9 0.8 0.2) c6\n",
    "shadorial-12": "-- Shadorial Chapter 12: Noise\n-- 2D value noise, FBM (6 octaves), and double domain warping\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (p''.x * p''.y)\n  where\n    p'  = fract (p * vec2 123.34 456.21)\n    p'' = p' + vec2 (dot p' (p' + vec2 45.32 45.32)) (dot p' (p' + vec2 45.32 45.32))\n\nvalueNoise : Vec 2 F32 -> F32\nvalueNoise p = mix (mix a b u.x) (mix c d u.x) u.y\n  where\n    i = floor p\n    f = fract p\n    u = f * f * (vec2 3.0 3.0 - f * splat2 2.0)\n    a = hash2d (i + vec2 0.0 0.0)\n    b = hash2d (i + vec2 1.0 0.0)\n    c = hash2d (i + vec2 0.0 1.0)\n    d = hash2d (i + vec2 1.0 1.0)\n\nfbm : Vec 2 F32 -> F32\nfbm p = v0 + v1 + v2 + v3 + v4 + v5\n  where\n    v0 = 0.5 * valueNoise p\n    v1 = 0.25 * valueNoise (p * splat2 2.0)\n    v2 = 0.125 * valueNoise (p * splat2 4.0)\n    v3 = 0.0625 * valueNoise (p * splat2 8.0)\n    v4 = 0.03125 * valueNoise (p * splat2 16.0)\n    v5 = 0.015625 * valueNoise (p * splat2 32.0)\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let uv       = fragCoord / resolution\n      aspect   = resolution.x / resolution.y\n      uv'      = vec2 (uv.x * aspect) uv.y\n      t        = time * 0.3\n      q        = vec2 (fbm (uv' * splat2 3.0 + vec2 0.0 0.0 + vec2 (t * 0.4) (t * 0.4)))\n                       (fbm (uv' * splat2 3.0 + vec2 5.2 1.3 + vec2 (t * 0.3) (t * 0.3)))\n      r        = vec2 (fbm (uv' * splat2 3.0 + q * splat2 4.0 + vec2 1.7 9.2 + vec2 (t * 0.15) (t * 0.15)))\n                       (fbm (uv' * splat2 3.0 + q * splat2 4.0 + vec2 8.3 2.8 + vec2 (t * 0.12) (t * 0.12)))\n      f        = fbm (uv' * splat2 3.0 + r * splat2 4.0)\n      color    = mix (vec3 0.05 0.02 0.15) (vec3 0.1 0.3 0.6) f\n      color'   = mix color (vec3 0.8 0.4 0.1) (dot q q * 0.8)\n      color''  = mix color' (vec3 0.95 0.85 0.6) (r.x * r.y * 0.6)\n      center   = fragCoord / resolution - vec2 0.5 0.5\n      vignette = 1.0 - 0.4 * dot center center\n      final    = color'' * splat3 vignette\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-13": "-- Shadorial Chapter 13: Particles\n-- GPU particle system with glow, lifecycle, and tone mapping\n\nhash : F32 -> F32\nhash n = fract (sin n * 43758.5453123)\n\nhash2 : F32 -> Vec 2 F32\nhash2 n = vec2 (hash n) (hash (n + 57.0))\n\nhash3 : F32 -> Vec 3 F32\nhash3 n = vec3 (hash n) (hash (n + 57.0)) (hash (n + 113.0))\n\n-- Compute a single particle's color contribution\nparticle : Vec 2 F32 -> F32 -> F32 -> F32 -> Vec 3 F32\nparticle uv id time aspect = pColor' * splat3 (glow * fade * 0.02)\n  where\n    basePos = hash2 (id * 17.31) * splat2 2.0 - vec2 1.0 1.0\n    bx      = basePos.x * aspect\n    speed   = 0.1 + hash (id * 7.13) * 0.3\n    size    = 0.004 + hash (id * 3.77) * 0.008\n    life    = mod (time * speed + hash (id * 11.0) * 10.0) 2.0\n    px      = bx + sin (time * 0.5 + id) * 0.05\n    py      = mod (basePos.y + life - 1.0 + 0.6) 1.4 - 0.7\n    pos     = vec2 px py\n    d       = length (uv - pos)\n    glow    = clamp (size / (d * d + 0.0001)) 0.0 3.0\n    fade    = smoothstep 0.0 0.2 life * smoothstep 2.0 1.5 life\n    pc      = hash3 (id * 31.17)\n    pColor  = mix (vec3 0.8 0.4 0.1) (vec3 0.3 0.6 1.0) pc.x\n    pColor' = mix pColor (vec3 1.0 0.8 0.3) (pc.y * 0.3)\n\n-- NOTE: Full 80-particle loop requires loop support.\n-- Here we demonstrate with a representative subset.\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let halfRes = resolution / splat2 2.0\n      uv      = (fragCoord - halfRes) / splat2 resolution.y\n      aspect  = resolution.x / resolution.y\n      color   = vec3 0.02 0.02 0.05\n              + particle uv 0.0 time aspect\n              + particle uv 1.0 time aspect\n              + particle uv 2.0 time aspect\n              + particle uv 3.0 time aspect\n              + particle uv 4.0 time aspect\n              + particle uv 5.0 time aspect\n              + particle uv 6.0 time aspect\n              + particle uv 7.0 time aspect\n              + particle uv 8.0 time aspect\n              + particle uv 9.0 time aspect\n              + particle uv 10.0 time aspect\n              + particle uv 11.0 time aspect\n              + particle uv 12.0 time aspect\n              + particle uv 13.0 time aspect\n              + particle uv 14.0 time aspect\n              + particle uv 15.0 time aspect\n              + particle uv 16.0 time aspect\n              + particle uv 17.0 time aspect\n              + particle uv 18.0 time aspect\n              + particle uv 19.0 time aspect\n      mapped  = color / (color + vec3 1.0 1.0 1.0)\n      vig     = fragCoord / resolution - vec2 0.5 0.5\n      final   = mapped * splat3 (1.0 - 0.5 * dot vig vig)\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-14": "-- Shadorial Chapter 14: Fluid (Water)\n-- Water surface simulation with layered waves, Fresnel, caustics, and foam\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (sin (dot p (vec2 127.1 311.7)) * 43758.5453123)\n\nnoise2d : Vec 2 F32 -> F32\nnoise2d p = mix (mix a b u.x) (mix c d u.x) u.y\n  where\n    i = floor p\n    f = fract p\n    u = f * f * (vec2 3.0 3.0 - f * splat2 2.0)\n    a = hash2d i\n    b = hash2d (i + vec2 1.0 0.0)\n    c = hash2d (i + vec2 0.0 1.0)\n    d = hash2d (i + vec2 1.0 1.0)\n\nwaterHeight : Vec 2 F32 -> F32 -> F32\nwaterHeight p t = h0 + h1 + h2 + h3 + h4 + h5\n  where\n    h0 = sin (p.x * 1.5 + t * 0.8) * 0.3\n    h1 = sin (p.y * 1.2 + t * 0.6) * 0.25\n    h2 = sin (dot p (vec2 0.7 0.9) * 3.0 + t * 1.2) * 0.12\n    h3 = sin (dot p (vec2 -0.5 0.8) * 4.0 + t * 1.5) * 0.08\n    h4 = noise2d (p * splat2 6.0 + vec2 (t * 0.5) (t * 0.5)) * 0.1\n    h5 = noise2d (p * splat2 12.0 - vec2 (t * 0.7) (t * 0.7)) * 0.05\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let uv        = fragCoord / resolution\n      p         = (uv - vec2 0.5 0.5) * splat2 4.0\n      h         = waterHeight p time\n      eps       = 0.01\n      hx        = waterHeight (p + vec2 eps 0.0) time\n      hy        = waterHeight (p + vec2 0.0 eps) time\n      nx        = (h - hx) / eps\n      ny        = (h - hy) / eps\n      normal    = normalize (vec3 nx ny 1.0)\n      viewDir   = vec3 0.0 0.0 1.0\n      reflected = reflect (negate viewDir) normal\n      skyColor  = mix (vec3 0.6 0.7 0.9) (vec3 0.2 0.4 0.8) (reflected.y * 0.5 + 0.5)\n      fresnel   = pow (1.0 - max (dot normal viewDir) 0.0) 4.0\n      waterCol  = vec3 0.0 0.15 0.3\n      surface   = mix waterCol skyColor (fresnel * 0.8)\n      caustic   = noise2d (p * splat2 8.0 + vec2 (time * 0.3) (time * 0.3))\n                * noise2d (p * splat2 12.0 - vec2 (time * 0.2) (time * 0.2))\n      surface'  = surface + vec3 0.2 0.3 0.4 * splat3 (caustic * (1.0 - fresnel))\n      sunDir    = normalize (vec3 0.5 0.8 1.0)\n      specAngle = max (dot reflected sunDir) 0.0\n      specular  = pow specAngle 128.0\n      surface'' = surface' + vec3 1.0 0.95 0.8 * splat3 (specular * 0.8)\n      foam      = smoothstep 0.3 0.5 h\n      final     = mix surface'' (vec3 0.9 0.95 1.0) (foam * 0.4)\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-15": "-- Shadorial Chapter 15: Fluid (Smoke)\n-- Smoke simulation using FBM turbulence with buoyancy\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (sin (dot p (vec2 127.1 311.7)) * 43758.5453123)\n\nnoise2d : Vec 2 F32 -> F32\nnoise2d p = mix (mix (hash2d i) (hash2d (i + vec2 1.0 0.0)) u.x)\n                (mix (hash2d (i + vec2 0.0 1.0)) (hash2d (i + vec2 1.0 1.0)) u.x)\n                u.y\n  where\n    i = floor p\n    f = fract p\n    u = f * f * (vec2 3.0 3.0 - f * splat2 2.0)\n\nfbm : Vec 2 F32 -> F32\nfbm p = v0 + v1 + v2 + v3 + v4 + v5\n  where\n    v0 = 0.5 * noise2d p\n    v1 = 0.25 * noise2d (p * splat2 2.0)\n    v2 = 0.125 * noise2d (p * splat2 4.0)\n    v3 = 0.0625 * noise2d (p * splat2 8.0)\n    v4 = 0.03125 * noise2d (p * splat2 16.0)\n    v5 = 0.015625 * noise2d (p * splat2 32.0)\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let uv       = fragCoord / resolution\n      aspect   = resolution.x / resolution.y\n      p        = vec2 (uv.x * aspect) uv.y\n      source   = vec2 (aspect * 0.5) 0.0\n      rise     = vec2 0.0 (time * 0.4)\n      turb     = vec2 (fbm (p * splat2 3.0 + rise)) (fbm (p * splat2 3.0 + rise + vec2 5.2 1.3))\n      distorted = p + turb * splat2 0.3\n      dx       = distorted.x - source.x\n      dy       = distorted.y\n      dist     = sqrt (dx * dx * 4.0 + dy * dy * 0.5)\n      density  = smoothstep 1.5 0.0 dist * smoothstep 0.0 0.1 uv.y\n      detail   = fbm (p * splat2 6.0 + rise * splat2 2.0) * 0.5 + 0.5\n      density' = density * detail\n      warmColor = vec3 0.8 0.4 0.1\n      coolColor = vec3 0.4 0.4 0.45\n      smokeCol  = mix warmColor coolColor uv.y\n      bgColor   = vec3 0.02 0.02 0.05\n      color     = mix bgColor smokeCol density'\n      center    = uv - vec2 0.5 0.5\n      vignette  = 1.0 - 0.5 * dot center center\n      final     = color * splat3 vignette\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-16": "-- Shadorial Chapter 16: Glitch\n-- RGB split, block displacement, scanlines, and noise overlay\n\nhash : F32 -> F32\nhash n = fract (sin n * 43758.5453123)\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (sin (dot p (vec2 127.1 311.7)) * 43758.5453123)\n\n-- Base pattern (gradient + shapes) that the glitch distorts\nbasePattern : Vec 2 F32 -> F32 -> Vec 3 F32\nbasePattern uv time = gradient + splat3 (stripe * 0.15)\n  + splat3 (dots * 0.1)\n  where\n    gradient = mix (vec3 0.1 0.2 0.4) (vec3 0.4 0.1 0.3) uv.y\n    stripe   = step 0.4 (fract (uv.x * 8.0 + time * 0.5))\n    dotDist  = length (fract (uv * splat2 4.0) - vec2 0.5 0.5)\n    dots     = smoothstep 0.3 0.25 dotDist\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let uv          = fragCoord / resolution\n      glitchTime  = floor (time * 8.0)\n      glitchOn    = step 0.7 (hash glitchTime)\n      blockY      = floor (uv.y * 20.0) / 20.0\n      blockRand   = hash (blockY * 100.0 + glitchTime)\n      blockShift  = (blockRand - 0.5) * 0.1 * glitchOn\n      uvShifted   = vec2 (uv.x + blockShift) uv.y\n      splitAmount = 0.015 * glitchOn\n      rUv         = vec2 (uvShifted.x + splitAmount) uvShifted.y\n      bUv         = vec2 (uvShifted.x - splitAmount) uvShifted.y\n      rChannel    = (basePattern rUv time).x\n      gChannel    = (basePattern uvShifted time).y\n      bChannel    = (basePattern bUv time).z\n      color       = vec3 rChannel gChannel bChannel\n      scanline    = 0.95 + 0.05 * sin (uv.y * resolution.y * 3.14159)\n      color'      = color * splat3 scanline\n      noiseVal    = hash2d (uv * resolution + vec2 (time * 1000.0) (time * 1000.0))\n      color''     = mix color' (vec3 noiseVal noiseVal noiseVal) (0.05 * glitchOn)\n      lineFlash   = step 0.95 (hash (blockY * 50.0 + glitchTime * 3.0)) * glitchOn\n      final       = color'' + splat3 (lineFlash * 0.3)\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-17": "-- Shadorial Chapter 17: Geometric\n-- Kaleidoscope patterns with domain repetition and SDF shapes\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (sin (dot p (vec2 127.1 311.7)) * 43758.5453123)\n\nrotate2d : F32 -> Vec 2 F32 -> Vec 2 F32\nrotate2d angle p = vec2 (p.x * c - p.y * s) (p.x * s + p.y * c)\n  where\n    c = cos angle\n    s = sin angle\n\nsdBox : Vec 2 F32 -> Vec 2 F32 -> F32\nsdBox p b = length (max d (vec2 0.0 0.0)) + min (max d.x d.y) 0.0\n  where\n    d = abs p - b\n\npalette : F32 -> Vec 3 F32\npalette t = a + b * cos ((c * splat3 t + d) * splat3 6.28318)\n  where\n    a = vec3 0.5 0.5 0.5\n    b = vec3 0.5 0.5 0.5\n    c = vec3 1.0 1.0 1.0\n    d = vec3 0.263 0.416 0.557\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let halfRes  = resolution / splat2 2.0\n      uv       = (fragCoord - halfRes) / splat2 resolution.y\n      angle    = atan uv.y uv.x\n      r        = length uv\n      kAngle   = mod angle (3.14159 / 4.0)\n      kAngle'  = abs (kAngle - 3.14159 / 8.0)\n      p        = vec2 (r * cos kAngle') (r * sin kAngle')\n      zoom     = 2.0 + sin (time * 0.5) * 0.5\n      p'       = p * splat2 zoom\n      cellId   = floor p'\n      cellP    = fract p' - vec2 0.5 0.5\n      cellAngle = hash2d cellId * 6.28 + time\n      cellP'   = rotate2d cellAngle cellP\n      boxD     = sdBox cellP' (vec2 0.15 0.15)\n      circD    = length cellP' - 0.12\n      d        = min boxD circD\n      col      = palette (hash2d cellId + time * 0.3)\n      shape    = smoothstep 0.02 0.0 d\n      color    = col * splat3 shape\n      glow     = 0.02 / (r + 0.1)\n      final    = color + vec3 0.3 0.5 0.8 * splat3 (glow * 0.3)\n  in vec4 final.x final.y final.z 1.0\n",
    "shadorial-18": "-- Shadorial Chapter 18: Pixel Art\n-- Pixelation, Bayer dithering, parallax hills, star field\n\nhash : F32 -> F32\nhash n = fract (sin n * 43758.5453123)\n\nhash2d : Vec 2 F32 -> F32\nhash2d p = fract (sin (dot p (vec2 127.1 311.7)) * 43758.5453123)\n\n-- 4x4 Bayer dithering matrix value\nbayer4x4 : Vec 2 F32 -> F32\nbayer4x4 coord = fract (sin (idx * 0.7853) * 43758.5453) -- Approximation\n  where\n    x   = mod coord.x 4.0\n    y   = mod coord.y 4.0\n    idx = x + y * 4.0\n    -- Approximate Bayer matrix lookup using step functions\n\n-- Hill function for parallax layers\nhill : F32 -> F32 -> F32 -> F32\nhill x offset height =\n  height * (0.5 + 0.3 * sin (x * 1.5 + offset)\n              + 0.2 * sin (x * 3.0 + offset * 2.0))\n\nshade : Vec 2 F32 -> F32 -> Vec 2 F32 -> Vec 4 F32\nshade fragCoord time resolution =\n  let pixelSize = 4.0\n      pixelGrid = splat2 pixelSize\n      pixCoord  = floor (fragCoord / pixelGrid) * pixelGrid\n      uv        = pixCoord / resolution\n      skyTop    = vec3 0.05 0.05 0.2\n      skyBottom = vec3 0.3 0.15 0.4\n      sky       = mix skyBottom skyTop uv.y\n      sunPos    = vec2 0.7 0.75\n      sunDist   = length (uv - sunPos)\n      sunGlow   = smoothstep 0.15 0.0 sunDist\n      sunColor  = vec3 1.0 0.8 0.3\n      baseColor = sky + sunColor * splat3 sunGlow\n      starCoord = floor (pixCoord / pixelGrid)\n      starRand  = hash2d starCoord\n      starBright = step 0.98 starRand\n      twinkle   = 0.5 + 0.5 * sin (time * 3.0 + starRand * 100.0)\n      starMask  = starBright * twinkle * step 0.5 uv.y\n      color     = baseColor + splat3 starMask\n      hx        = uv.x\n      hill1     = hill (hx * 4.0 + time * 0.02) 0.0 0.3\n      hill2     = hill (hx * 6.0 + time * 0.05) 2.0 0.2\n      hill3     = hill (hx * 8.0 + time * 0.1) 4.0 0.15\n      hillCol1  = vec3 0.05 0.15 0.05\n      hillCol2  = vec3 0.08 0.2 0.08\n      hillCol3  = vec3 0.1 0.25 0.1\n      sceneColor = if uv.y < hill1 then hillCol1\n                   else if uv.y < hill2 + 0.15 then hillCol2\n                   else if uv.y < hill3 + 0.3 then hillCol3\n                   else color\n      dither    = bayer4x4 (fragCoord / pixelGrid)\n      quantized = floor (sceneColor * splat3 5.0 + splat3 (dither * 0.5)) / splat3 5.0\n  in vec4 quantized.x quantized.y quantized.z 1.0\n",
    "shadorial-19": "-- Shadorial Chapter 19: Three.js Interactive\n-- Custom vertex/fragment shader with displacement and Fresnel rim\n-- Note: In the original this runs through Three.js ShaderMaterial\n--       with varyings from a custom vertex shader.\n\nshade : Vec 3 F32 -> Vec 2 F32 -> F32 -> F32 -> Vec 4 F32\nshade normal uv time displacement =\n  let n         = normalize normal\n      baseColor = n * splat3 0.5 + vec3 0.5 0.5 0.5\n      pulse     = sin (time * 2.0) * 0.5 + 0.5\n      color1    = vec3 0.2 0.5 1.0\n      color2    = vec3 1.0 0.3 0.5\n      dynColor  = mix color1 color2 pulse\n      color     = mix baseColor dynColor 0.5\n      highlight = smoothstep 0.0 0.3 displacement\n      color'    = color + vec3 0.3 0.4 0.5 * splat3 highlight\n      viewDir   = vec3 0.0 0.0 1.0\n      rim       = 1.0 - max (dot n viewDir) 0.0\n      rim'      = pow rim 3.0\n      color''   = color' + vec3 0.4 0.6 1.0 * splat3 (rim' * 0.6)\n  in vec4 color''.x color''.y color''.z 1.0\n"
};

